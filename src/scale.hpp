// scale.hpp -- the default type scale shared by the reading view
// and the search bar chrome, relative to the application font; the
// zoom domains multiply on top of it (see MainWin::applyZoom).
// Make this configurable when settings land.
#ifndef SRTVIEW_SRC_SCALE_HPP_
#define SRTVIEW_SRC_SCALE_HPP_

inline constexpr double kFontScale = 1.75;

#endif // SRTVIEW_SRC_SCALE_HPP_
